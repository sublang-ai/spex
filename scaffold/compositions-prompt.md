<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

Fill in @specs/compositions/ using @specs/meta.md, especially META-16, META-20–META-22, and META-28–META-35:

- Audit every package Verification item and peer citation before composing. Retain an exact peer `## External Behavior` citation when it expresses an intentional fixed semantic dependency; move checks that require installed selections into composition Verification, and preserve each product-meaningful installed relationship as a Binding or Scenario rather than silently dropping it.
- Find External assembly roles, load-bearing External state for installation policy, and complete consumed requirements in package `## Internal Behavior`. Match assembly roles and policy inputs to External outcomes and supply requirements to External outcomes or selected services; do not bind private invariants or implementation allocation. Treat one provider as the default and make every optional, disabled, aggregate, fallback, multi-provider, or runtime-selected case explicit in its client contract.
- Record each atomic installed selection, assembly, or policy once in a static Binding item. Use the `Where` clause as the sole source of exact profile, instance, applicability, and client roles; use the shall clause as the sole source of provision, supplier roles, and load-bearing External inputs. Put exact package citations beside those roles, or name an external service's exact selected capability and cite its decision. Do not add `Clients:`, `Suppliers:`, or `Scope:` lines, and do not use `While` or `When` in a Binding.
- Make n:m mapping explicit in the clauses or an adjacent table; never imply pairing by citation order. Split endpoints or scopes that the installation owner intends to select, change, or verify independently. For every closed installation profile, reject missing or competing effective relationships unless the client contract permits that cardinality. Keep package files unchanged and omit replaceable code dependencies.
- Write Scenario items for integrated outcomes requiring materially necessary behavior from two or more packages. Inline-cite each behavior beside the exact causal phrase, including at least one External item grounding the system outcome; cite Internal items only when necessary to state or inspect that outcome, without treating them as exposed package contracts. Inline-cite each materially exercised Binding at its handoff.
- Use a mixed file only when its bindings directly serve the same cohesive scenarios. Put cross-cutting bindings in binding-only files; use scenario-only files where no new binding is owned.
- Permit only direct operative item citations in Scenario and Verification—never background, transitive, supporting, navigation, or mere-call links.
- Add same-file Verification for every Binding and Scenario, inline-citing each target beside the assertion that directly checks it. Binding checks prove compatibility, selection, scope, and mapping; exercise a relationship observable outside the installed system boundary at that external surface, and use conformance or deployment inspection for an installation-hidden relationship. Scenario checks prove integrated outcomes. Do not specify unit tests.
- Treat directories below @specs/compositions/ as navigation-only collections and name files after the installed concern or outcome, not concatenated package names.
- List new files in the ## Compositions section of @specs/map.md, then validate exact links and the applicable META rules with format-compatible checks; never restore removed trace lines to satisfy an older checker.
